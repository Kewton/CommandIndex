# オーケストレーション実行計画

- **日付**: 2026-03-19
- **対象Issue**: #9, #10, #11

## 対象Issue一覧

| Issue | タイトル | 複雑度 |
|-------|---------|--------|
| #9 | search コマンド実装（全文検索・フィルタ） | 高 |
| #10 | status コマンド実装 | 低 |
| #11 | clean コマンド実装 | 低 |

## 依存関係グラフ

```
#4(済) --+
#7(済) --+-- #9 (search)
#8(済) --+
#6(済) --+-- #10 (status)
         +-- #11 (clean)
```

Issue間の直接依存はなし。全て既にマージ済みのIssueにのみ依存。

## 並列実行グループ

- **Group A (並列)**: #9, #10, #11 -- 全て同時開発可能

## 共通ファイル（コンフリクトリスク）

| ファイル | #9 | #10 | #11 | リスク |
|---------|-----|-----|-----|--------|
| src/main.rs | Search arm実装 | Status arm実装 | Clean arm実装 | 低（異なるarm） |
| src/cli/mod.rs | search追加 | status追加 | clean追加 | 低（異なるmodule宣言） |
| src/lib.rs | search module追加 | - | - | なし |

## マージ推奨順序

1. #11 (clean) -- 最小変更
2. #10 (status) -- 小変更
3. #9 (search) -- 最大変更（コンフリクト解消が容易になる順）
